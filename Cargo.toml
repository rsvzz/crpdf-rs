[package]
name = "crpdf"
version = "0.1.0"
edition = "2024"

[dependencies]

[build-dependencies]
cmake = "0.1.58"
pkg-config = "0.3.34"

[profile.release]
opt-level = "z"
lto = true
incremental = false
codegen-units = 1
strip = true
panic = "abort"